import 'dart:math';

class ArrowAnchor {
  Point? anchorStart;
  Point? anchorEnd;
  Point? arrowControlPoint;

  ArrowAnchor({required this.anchorStart, required this.anchorEnd, required this.arrowControlPoint});
}
